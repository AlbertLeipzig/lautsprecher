/*   const testTagValue = (tag)  => {
    if (tag) {
      if (tag.includes('')) {
        tagsArray.push(tag);
      } else {
        tag.forEach((singleTag) => {
          tagsArray.push(singleTag);
        });
      }
    }
  }; */

  /* testTagValue(event.musicians); */

  /* if (event.musicians) {
    if (event.musicians.includes('')) {
      tagsArray.push(event.musicians);
    } else {
      event.musicians.forEach((musician) => {
        tagsArray.push(musician);
      });
    }
  } */